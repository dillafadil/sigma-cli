```diff
- function hitung(a, b) {
+ function hitung(a, b) {
```
```diff
- return a + b;
+ return a - b; // Mengubah tipe operasi untuk menambah
```diff
- function sapa(nama) {
+ function sapa(nama) {
```